apiVersion: v1
kind: Service
metadata:
  name: senre-frontend
  namespace: frontend-ns
spec:
  selector:
    app: senre-frontend
  ports:
    - name: http
      port: 80
      targetPort: 3000
  type: ClusterIP