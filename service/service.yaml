apiVersion: v1
kind: Service
metadata:
  name: crayon-service
  namespace: default
spec:
  selector:
    app: crayon-service
  ports:
    - name: http
      port: 9000
      targetPort: 9000
  type: ClusterIP