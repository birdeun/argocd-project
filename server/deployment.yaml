apiVersion: apps/v1
kind: Deployment
metadata:
  name: senre-server
  namespace: server-ns
spec:
  replicas: 1
  selector:
    matchLabels:
      app: senre-server
  template:
    metadata:
      labels:
        app: senre-server
    spec:
      containers:
        - name: senre-server
          image: senre/crayon-auth:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8000
          env:
            - name: NODE_ENV
              value: "production"
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 200m
              memory: 256Mi