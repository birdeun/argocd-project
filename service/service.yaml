apiVersion: v1
kind: Service
metadata:
  name: senre-service
  namespace: service-ns
spec:
  selector:
    app: senre-service
  ports:
    - name: http
      port: 9000
      targetPort: 9000
  type: ClusterIP