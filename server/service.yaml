apiVersion: v1
kind: Service
metadata:
  name: senre-server
  namespace: server-ns
spec:
  selector:
    app: senre-server
  ports:
    - name: http
      port: 8000
      targetPort: 8000
  type: ClusterIP