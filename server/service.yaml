apiVersion: v1
kind: Service
metadata:
  name: crayon-server
  namespace: default
spec:
  selector:
    app: crayon-server
  ports:
    - name: http
      port: 8000
      targetPort: 8000
  type: ClusterIP