apiVersion: apps/v1
kind: Deployment
metadata:
  name: crayon-frontend
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: crayon-frontend
  template:
    metadata:
      labels:
        app: crayon-frontend
    spec:
      containers:
        - name: crayon-frontend
          image: senre/crayon-frontend:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 200m
              memory: 256Mi