apiVersion: v1
kind: Service
metadata:
  name: crayon-frontend
  namespace: default
spec:
  selector:
    app: crayon-frontend
  ports:
    - name: http
      port: 80
      targetPort: 3000
  type: ClusterIP